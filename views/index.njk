<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <title>{{ page_title }}</title>
</head>

<body>

  {% for user in users %}
    <div class="module">
      <h3>{{ user.name }}</h3>
      <p>{{ user.description }}</p>
    </div>
  {% endfor %}

</body>

</html>